android_app {
    name: "EVSafe",
    srcs: ["src/**/*.java"],
    resource_dirs: ["res"],
    manifest: "AndroidManifest.xml",
    platform_apis: true,
    privileged: true, // 시스템 앱 권한 설정
    static_libs: [
        "android.car",
        "android-support-annotations",
    ],
    certificate: "platform",
}
